name: lcm-lora-fast
version: 0.1.0
description: "Fast LCM LoRA txt2img community workflow from thinkyhead gist"
author: community
category: image_generation
license: MIT

requires:
  python: ">=3.10"
  gpu: true
  vram_gb: 4
  disk_gb: 2
  deps: ["torch", "diffusers", "transformers"]

models: []

tools:
  - lcm_lora_fast

inputs: ["image"]
outputs: ["image"]

tags: ["lcm-lora-fast", "comfyui", "workflow"]
